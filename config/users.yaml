# ─────────────────────────────────────────────────────────────────
# User credentials for the RAG system demo.
#
# ⚠️  For production: replace plain-text passwords with bcrypt hashes
#     and use a proper identity provider (Auth0, Cognito, LDAP, etc.)
#
# Roles must match entries defined in config/config.yaml → roles.
# ─────────────────────────────────────────────────────────────────

users:
  alice:
    password: "alice123"
    role: "admin"
    display_name: "Alice (Admin)"

  bob:
    password: "bob123"
    role: "support"
    display_name: "Bob (Support)"

  guest:
    password: "guest123"
    role: "viewer"
    display_name: "Guest (Viewer)"
